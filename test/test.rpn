def one() {
	return 1;
}

def double(num) {
	return * num 2;
}

def f(a, b) {
	c = * + a one() b;
	d = - + a b / c ~ 5;
	return d;
}

>> a;
b = + a 1;
c = + b one();
d = double(b);

<< a;
<< b;
<< c;
<< d;

a = f(b, a);
b = + f(f(c, d), ~ / a c) a;

<< a;
<< b;

<< ! 6;

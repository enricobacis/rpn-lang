def one() {
	return 1;
}

def double(num) {
	return * num 2;
}

def f(a, b) {
	c = * + a 2 b;
	d = * + a b - c _ 5;
	return d;
}

program {
	a = one();
	b = + a 1;
	c = + b one();
	d = double(b);

	?a;
	?b;
	?c;
	?d;

	a = f(b, a);
	?a;
	b = + f(f(a, b), f(c, d)) a;
	?b;

	return 0;
}
